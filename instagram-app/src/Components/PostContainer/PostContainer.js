import React from 'react';

const PostContainer = props => {
    return (
        <>
            {props.dummy.map(dummy => {
                return (
                    <div>
                        hh
                    </div>
                );
            })}
        </>
    );
}

export default PostContainer;